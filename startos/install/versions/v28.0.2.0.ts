import { VersionInfo } from '@start9labs/start-sdk'

export const v_28_0_2_0 = VersionInfo.of({
  version: '28.0.2:0',
  releaseNotes: 'Initial release for StartOS 040',
  migrations: {
    up: async ({ effects }) => {},
    down: async ({ effects }) => {},
  },
})
